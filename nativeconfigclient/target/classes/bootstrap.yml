server:
  port:
spring:
  application:
    name: configclient
  profiles:
    active: dev
#和nativeconfigserver 中shared包下 的configclient-dev.yml对应（拼接查找）
  cloud:
    config:
#      本地的configserver访问路径
      uri: http://localhost:8762
#      设置客服端优先判断ConfigServer获取是否正常
      fail-fast: true