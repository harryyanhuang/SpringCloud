server:
  port: 8090
spring:
  application:
    name: zipkinclient
  sleuth:
    web:
      client:
        enabled: true
#        设置请求跟踪

    sampler:
      probability: 1.0
#      设置采样比例，默认1.0
  zipkin:
    base-url: http://localhost:9090/
eureka:
  client:
    service-url:
      defualtZone: http://localhost:8761/eureka/
