server:
  port: 8761
eureka:
  client:
    ##是否注册自己（是否在注册中心把自己注册为微服务,即是否将当前的eureka server服务作为客户端进行注册）
    register-with-eureka: false
    ##是否同步其他注册中心（获取其他eureka server服务的数据）
    fetch-registry: false
    ##访问地址
    service-url:
     defaultZone:  http://localhost:8761/eureka/
